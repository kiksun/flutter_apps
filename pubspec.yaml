name: flutter_template
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.1.1
  dio: ^5.1.2
  retrofit: ^4.0.1
  json_annotation: ^4.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.2
  retrofit_generator: ^6.0.0+3
  json_serializable: ^6.0.3

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
